Project Overview:
Fits and tests a two layer and one layer feed forward neural network with manual calculation of gradients to predict wine quality from the UCI Wine Dataset

Run Instructions:
python3 main.py will fit the model and print out the model test accuracy